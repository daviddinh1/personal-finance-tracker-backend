spring.application.name=personal-finance-tracker-backend
spring.datasource.url=jdbc:postgresql://localhost:5432/financetracker
spring.datasource.username=daviddinh6003
spring.datasource.password=CHANGEINPROD!
spring.datasource.driver-class-name=org.postgresql.Driver
spring.sql.init.mode=always

# JPA (if you?re using it):
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Generate a 256-bit (32 byte) secret and Base64-encode it for HS256
app.jwt.secret=tqq3zqKOwX0a9judR9HinwntukEvy6NLEE/6vQtmFnE=
# Token validity in milliseconds (e.g. 1h = 3_600_000ms)
app.jwt.expiration-ms=3600000

# ??? Jackson / Date Handling ??????????????????????????????????????????????
# Keep java.time types as ISO strings rather than timestamps
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false